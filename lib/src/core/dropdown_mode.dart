/// Controls how the dropdown options are presented to the user.
enum DropdownMode {
  /// Shows options in a modal bottom sheet.
  bottomSheet,

  /// Shows options inside a centered dialog.
  dialog,

  /// Shows options in an anchored overlay below the field.
  overlay,
}
